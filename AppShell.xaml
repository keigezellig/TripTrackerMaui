<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TripTracker.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:TripTracker.Views">

    <TabBar>
        <Tab Title="Home">
            <ShellContent 
                     ContentTemplate="{DataTemplate local:MainPage}"
                      Route="MainPage"/>
        </Tab>
        <Tab Title="Data">
            <ShellContent 
                     ContentTemplate="{DataTemplate local:LiveDataPage}" 
                      Route="DataPage"/>
        </Tab>
        <Tab Title="Map">
            <ShellContent 
                     ContentTemplate="{DataTemplate local:MapPage}"
                      Route="MapPage"/>
        </Tab>
        <Tab Title="Settings">
            <ShellContent 
                ContentTemplate="{DataTemplate local:SettingsPage}"
                Route="SettingsPage"/>
        </Tab>
    </TabBar>
    

</Shell>
