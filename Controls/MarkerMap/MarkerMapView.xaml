<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Name="MarkerMapViewThingie"
             x:Class="MauiApp1.Controls.MarkerMap.MarkerMapView">
    <HorizontalStackLayout BindingContext="{x:Reference MarkerMapViewThingie}">
        <Label Text="{Binding Path=MarkerCollection.Count, FallbackValue='-1'}"/>
        <CollectionView ItemsSource="{Binding Path=MarkerCollection}">
            <CollectionView.ItemTemplate>
                
            </CollectionView.ItemTemplate>
            
        </CollectionView>
    </HorizontalStackLayout>
    
</ContentView>